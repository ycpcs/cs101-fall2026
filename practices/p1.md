---
layout: default
title: "Practice for L1 & L2"
---

Text in **bold** is the user input.

# Practice 1: Read and Repeat

Write a program that asks for one decimal number and prints it with three digits after the decimal point.

### Example output

```text
Enter a decimal value: **6.25**
You entered 6.250
```
### Starter code

```cpp
#include <stdio.h>

int main()
{
    double value;

    printf("Enter a decimal value: ");
    // Read value here

    // Print value here

    return 0;
}
```

---

## Practice 2: Add Two Decimals

Ask the user for two `double` values and display their sum.

### Example output

```text
Enter first decimal value: **3.5**
Enter second decimal value: **2.25**
3.500 + 2.250 = 5.750
```

### Requirements

- Use two variables of type `double`.
- Use `%lf` with `scanf`.
- Use `%.3f` with `printf`.

---

## Practice 3: Rectangle Calculator

Ask for the length and width of a rectangle. Print its area and perimeter.

### Formulas

```text
area = length * width
perimeter = 2 * (length + width)
```

### Example output

```text
Enter length: **5.5**
Enter width: **2**
Area: 11.000
Perimeter: 15.000
```

---

## Practice 4: Temperature Converter

Ask for a temperature in Fahrenheit and convert it to Celsius.

### Formula

```text
celsius = (fahrenheit - 32.0) * 5.0 / 9.0
```

### Example output

```text
Enter temperature in Fahrenheit: **68**
Temperature in Celsius: 20.000
```

### Think about it

Why does the formula use `32.0`, `5.0`, and `9.0` instead of only integers?

---

## Practice 5: Integer Division

Ask for two integers and print their integer quotient.

### Example output

```text
Enter first integer: **17**
Enter second integer: **5**
17 / 5 = 3
```

### Think about it

Why is the answer `3` instead of `3.4`?

---

## Practice 6: Quotient and Remainder

Ask for a number of cookies and a number of students. Calculate how many cookies each student receives and how many remain.

### Example output

```text
Enter number of cookies: **23**
Enter number of students: **5**
Each student receives 4 cookies.
There are 3 cookies left.
```

### Operators to use

```cpp
cookies / students
cookies % students
```

---

## Practice 7: Convert Minutes

Ask for a total number of minutes. Convert it into hours and remaining minutes.

### Example output

```text
Enter total minutes: **135**
135 minutes is 2 hours and 15 minutes.
```

### Hint

```cpp
hours = totalMinutes / 60;
remainingMinutes = totalMinutes % 60;
```

---

## Practice 8: Fix the Bugs

The following program contains several mistakes. Find and correct them.

```cpp
#include <stdio.h>

int main()
{
    double price;
    int quantity;

    printf("Enter the price: ");
    scanf("%f", price);

    printf("Enter the quantity: ");
    scanf("%lf", &quantity);

    printf("Total: %.3i\n", price * quantity);

    return 0;
}
```

Look carefully at:

- The conversion specifier for reading a `double`
- The conversion specifier for reading an `int`
- The use of `&`
- The conversion specifier for printing the result
- The type of the expression `price * quantity`

---

## Practice 9: Mini Calculator

Create a smaller version of the lab. Read two `double` values and print only their product and quotient.

### Example output

```text
Enter first value: **9**
Enter second value: **4**
9.000 x 4.000 = 36.000
9.000 / 4.000 = 2.250
```

---

## Practice 10: Full Lab Rehearsal

Write a program that:

1. Reads two `double` values.
2. Prints their sum and product.
3. Reads two `int` values.
4. Prints their quotient and remainder.
5. Prints a closing message.

Use different wording and values from the actual lab so you practice the concepts rather than memorizing the solution.

### Example output

```text
Enter two decimal values: **12.5** **2.0**
Sum: 14.500
Product: 25.000

Enter a number of items: **29**
Enter a group size: **6**
Complete groups: 4
Items remaining: 5

Calculations complete.
```

---

## Short Concept Questions

1. What is the difference between `int` and `double`?
2. Why does `scanf` normally need `&` before a variable?
3. Which conversion specifier reads a `double`?
4. Which conversion specifier prints a `double`?
5. What does `%.3f` mean?
6. What does the `%` arithmetic operator calculate?
7. How do you print a literal percent sign with `printf`?
8. What result does `11 / 4` produce when both values are integers?
9. What result does `11.0 / 4.0` produce?
10. What problem could occur if the second value in a division is zero?

---
