---
layout: default
title: "Practice for L3"
---


# Additional Practice: Variables and Expressions

## Coding Practice

### 1. Changing Variables

Write a program that:

1. Stores `10` in an integer variable named `count`.
2. Adds `5` using `+=`.
3. Increments it using `++`.
4. Subtracts `3` using `-=`.
5. Prints the final value.

Expected output:

```text
Final count: 13
```

---

### 2. Rectangle

Write a program that reads a rectangle's length and width as `double` values. Calculate its area and perimeter.

Use the following formulas:

$$
\text{area} = \text{length} \times \text{width}
$$

$$
\text{perimeter} = 2 \times (\text{length} + \text{width})
$$

Print each result with two digits after the decimal point.

Example:

```text
Enter the length: 7.5
Enter the width: 3.0

Area: 22.50
Perimeter: 21.00
```

---

### 3. Class Average

Write a program that reads three integer test scores and calculates their average as a `double`.

Example:

```text
Enter score 1: 80
Enter score 2: 81
Enter score 3: 81

Average: 80.67
```

Use a type cast or a mixed-type expression so that the fractional part of the average is not lost.

---

### 4. Temperature Conversion

Write a program that reads a Fahrenheit temperature as a `double` and converts it to Celsius.

Use the following formula:

$$
C = (F - 32) \times \frac{5}{9}
$$

Example:

```text
Enter a Fahrenheit temperature: 77
Celsius temperature: 25.0
```

Be careful to avoid integer division.

---

### 5. Distance from the Origin

Write a program that reads the `x` and `y` coordinates of a point and calculates the point's distance from the origin.

Use the following formula:

$$
d = \sqrt{x^2 + y^2}
$$

Remember to include the `math.h` header file:

```c
#include <math.h>
```

C does not use `^` to calculate a power. Calculate \(x^2\) as:

```c
x * x
```

Example:

```text
Enter x: 3.0
Enter y: 4.0

Distance from the origin: 5.00
```

---

### 6. Right-Triangle Extension

Modify the Right Triangle program so that it also calculates the triangle's area and perimeter.

Use the following formulas:

$$
\text{area} = \frac{a \times b}{2}
$$

$$
\text{perimeter} = a + b + \text{hypotenuse}
$$

Print all measurements with one digit after the decimal point.

Example:

```text
Enter the length of side 1: 3.0
Enter the length of side 2: 4.0

Side 1: 3.0
Side 2: 4.0
Hypotenuse: 5.0
Area: 6.0
Perimeter: 12.0
```

---

# Debugging Practice

Each of the following code fragments contains an error. Identify the error and correct it.

## A. Reading a `double`

```c
double length;
scanf("%f", length);
```

## B. Printing a Result

```c
double result = 4.5;
print("Result: %f\n", result);
```

## C. Integer Division

```c
int total = 7;
int count = 2;
double average = total / count;
```

## D. Computing a Square

```c
double square = x ^ 2;
```

## E. Calling `sqrt`

```c
double answer = sqrt 25.0;
```

## F. Printing One Decimal Place

```c
printf("Length: %1f\n", length);
```

---

# Debugging Answer Key

## A. Reading a `double`

```c
double length;
scanf("%lf", &length);
```

The `%lf` conversion specifier is used to read a `double`. The `&` operator provides the address of the variable to `scanf`.

## B. Printing a Result

```c
double result = 4.5;
printf("Result: %f\n", result);
```

The correct function name is `printf`, not `print`.

## C. Integer Division

```c
int total = 7;
int count = 2;
double average = (double) total / count;
```

Casting `total` to `double` causes floating-point division to be performed.

## D. Computing a Square

```c
double square = x * x;
```

The `^` operator does not perform exponentiation in C.

## E. Calling `sqrt`

```c
double answer = sqrt(25.0);
```

A function's arguments must be placed inside parentheses.

## F. Printing One Decimal Place

```c
printf("Length: %.1f\n", length);
```

The `.1` specifies that one digit should be printed after the decimal point.
